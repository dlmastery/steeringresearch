# Safety-probe hello-world — Python dependencies.
# Tested with Python 3.10–3.12. Install into a fresh virtual environment:
#     python -m venv .venv
#     .venv\Scripts\activate      (Windows)      source .venv/bin/activate  (macOS/Linux)
#     pip install -r requirements.txt
#
# GPU is recommended (~2 GB VRAM in bf16) but everything runs on CPU too, slower.

# --- Core: the frozen LLM + tensors ---------------------------------------
# Install the CUDA build of torch from https://pytorch.org if you have a GPU.
torch>=2.2
transformers>=4.44
accelerate>=0.30          # device placement / from_pretrained helpers
huggingface_hub>=0.24     # hf_hub_download for the datasets + model cache

# --- Data + the probe -----------------------------------------------------
pandas>=2.0               # read the JailbreakBench / XSTest CSVs
scikit-learn>=1.3         # StandardScaler, LogisticRegression, the metric suite
numpy>=1.24

# --- Plots ----------------------------------------------------------------
matplotlib>=3.7           # ROC / PR / calibration / confusion PNGs (Agg backend)

# --- Webapp ---------------------------------------------------------------
fastapi>=0.110            # the /predict + /metrics demo server
uvicorn>=0.29             # ASGI server that runs the FastAPI app

# --- Networking (optional but recommended) --------------------------------
truststore>=0.9           # verify TLS against the OS trust store (corporate SSL proxies)

# --- Optional: 4-bit quantized loading ------------------------------------
# Only needed if you switch model_utils.load_model() to load_in_4bit for a
# larger swap-in model (e.g. the 12B). The 1B we ship loads fine in bf16.
# bitsandbytes>=0.43      # (uncomment; Linux/WSL wheels are the most reliable)
