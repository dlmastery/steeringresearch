# E45 — Provenance & Tracing

> **Title:** HyperSteer zero-shot  
> **Verdict (IDEA_TABLE.md):** INCONCLUSIVE(scr): LOO held-out cos~=0 (mean -0.02, std 0.61) at n=4; projection-efficacy proxy unreliable, needs more behaviors+real judge (exp#111)  
> **Models touched:** gemma-3-270m-it  
> **Experiments:** 1 rows — exp# 111

_Auto-generated by `scripts/build_provenance.py` from `experiment_log.jsonl` + `reasoning_annotations.json` + `ideas/_campaigns/`. Do not hand-edit; re-run the generator._

> **This hypothesis trains a small auxiliary component** (a logistic gate / an MLP hypernetwork / a sparse autoencoder + an optimized steering vector) — the base model stays frozen. These are the project's only gradient-trained experiments; the rows below carry method-specific metrics (gate AUC / held-out cosine / Gram mass) in their `method_extra`, not the standard 5-axis composite. See [`../TRAINING-PROCESS.md`](../TRAINING-PROCESS.md) §4.

## 1. The exact experiments

| exp# | tag | model | layer | alpha | op | source | behavior | PPL | composite | off-shell |
|------|-----|-------|-------|-------|----|--------|----------|-----|-----------|-----------|
| 111 | `E45-hypersteer-loo` | gemma-3-270m-it | 9 | — | add | hypernet | 1.308 | 0.0 | -0.020 | 0.0000 |

## 2. How to reproduce

**Tag prefix `E45-hypersteer*`** — script `scripts/run_e45.py  (steering.hypersteer)`, artifact `ideas/_campaigns/E45-hypersteer.json`:

```bash
PYTHONPATH=src python scripts/run_e45.py --model models/google/gemma-3-270m-it --quant none # leave-one-behavior-out hypernetwork (Adam/MSE) description->vector cosine + efficacy
```

Each campaign cell pre-authors a `_manual` reasoning entry tied to this hypothesis, then calls `steering.runner.run_single_experiment` (model loaded once via `load_model_cached`); every cell appends one row to `experiment_log.jsonl` with the shared `composite_fingerprint`.

## 3. Artifacts

- Campaign result JSON: `ideas/_campaigns/E45-hypersteer.json` ([blob](https://github.com/dlmastery/steeringresearch/blob/master/ideas/_campaigns/E45-hypersteer.json))
- Experiment log (all rows, append-only): `autoresearch_results/experiment_log.jsonl` ([blob](https://github.com/dlmastery/steeringresearch/blob/master/autoresearch_results/experiment_log.jsonl)) — this hypothesis = exp# 111
- Reasoning annotations: `autoresearch_results/reasoning_annotations.json` ([blob](https://github.com/dlmastery/steeringresearch/blob/master/autoresearch_results/reasoning_annotations.json))
- Design doc: `hypotheses/F_robustness/E45_hypersteer_zeroshot.md` ([blob](https://github.com/dlmastery/steeringresearch/blob/master/hypotheses/F_robustness/E45_hypersteer_zeroshot.md))

## 4. Reasoning trace (representative)

Representative reasoning entry — **exp#111** (`autoresearch_results/reasoning_annotations.json` key `"111"`):

- **Diagnosis:** E45 was UNTESTED for lack of a hypernetwork trainer. The new steering.hypersteer module (an MLP trained by Adam on MSE regression from description embeddings to DiffMean vectors) now lets us screen whether a behavior's steering vector is predictable from its text description alone, at injection layer 9. Leave-one-behavior-out over the 4 available concepts; the unit test already proved generalization on a larger synthetic set.
- **Hypothesis:** Mechanism: because the description-embedding space and the steering-vector space are both low-dimensional and smoothly related, a small hypernetwork can learn the mapping and generalize to a held-out behavior it never saw a contrast set for. We predict held-out cosine > 0 and an efficacy ratio approaching the >= 0.70 supervised-efficacy threshold; this is the description-only (zero-contrast-set) workflow per HyperSteer.
- **Prediction:** Mean held-out cosine in [0.2, 0.9]; mean efficacy ratio in [0.3, 0.9]; SUPPORTED iff ratio >= 0.70. 4-behavior LOO is smoke-scale, n=1 SCREENING, fingerprint a9001e87087e.
- **Verdict:** DISCARD — composite -0.0202, behavior +1.3080, CR 0.0000 (global best remains +0.8336 tag=E3-cliff-a1.0)

## 5. Result & interpretation

Across these 1 screening cells the composite ranged -0.020..-0.020; perplexity 0.0..0.0; off-shell Δ‖h‖ 0.0000..0.0000. The IDEA_TABLE verdict for E45 is **INCONCLUSIVE(scr): LOO held-out cos~=0 (mean -0.02, std 0.61) at n=4; projection-efficacy proxy unreliable, needs more behaviors+real judge (exp#111)** — see the design doc's status journal and committee Q&A for the full interpretation and the pre-registered falsifier. These rows are n=1 SCREENING geometry probes (behavior is a synthetic-lexicon proxy; PPL and off-shell Δ‖h‖ are the real signals); the rung-3 generation-judge confirmation is the pending step.
