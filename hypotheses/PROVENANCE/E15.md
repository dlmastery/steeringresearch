# E15 — Provenance & Tracing

> **Title:** Learned gate vs fixed threshold  
> **Verdict (IDEA_TABLE.md):** FALSIFIED_OOD(scr): learned gate overfits tiny data, OOD AUC gap -0.17 (logistic 0.55 vs cosine 0.72), both perfect in-dist (exp#110)  
> **Models touched:** gemma-3-270m-it  
> **Experiments:** 1 rows — exp# 110

_Auto-generated by `scripts/build_provenance.py` from `experiment_log.jsonl` + `reasoning_annotations.json` + `ideas/_campaigns/`. Do not hand-edit; re-run the generator._

> **This hypothesis trains a small auxiliary component** (a logistic gate / an MLP hypernetwork / a sparse autoencoder + an optimized steering vector) — the base model stays frozen. These are the project's only gradient-trained experiments; the rows below carry method-specific metrics (gate AUC / held-out cosine / Gram mass) in their `method_extra`, not the standard 5-axis composite. See [`../TRAINING-PROCESS.md`](../TRAINING-PROCESS.md) §4.

## 1. The exact experiments

| exp# | tag | model | layer | alpha | op | source | behavior | PPL | composite | off-shell |
|------|-----|-------|-------|-------|----|--------|----------|-----|-----------|-----------|
| 110 | `E15-learned-gate` | gemma-3-270m-it | 4 | — | gate | logistic | 0.550 | 0.0 | -0.168 | 0.0000 |

## 2. How to reproduce

**Tag prefix `E15-learned-gate*`** — script `scripts/run_e15.py  (steering.gate)`, artifact `ideas/_campaigns/E15-learned-gate.json`:

```bash
PYTHONPATH=src python scripts/run_e15.py --model models/google/gemma-3-270m-it --quant none # trains the multi-layer logistic gate + fixed cosine baseline, in-dist + OOD PR-AUC
```

Each campaign cell pre-authors a `_manual` reasoning entry tied to this hypothesis, then calls `steering.runner.run_single_experiment` (model loaded once via `load_model_cached`); every cell appends one row to `experiment_log.jsonl` with the shared `composite_fingerprint`.

## 3. Artifacts

- Campaign result JSON: `ideas/_campaigns/E15-learned-gate.json` ([blob](https://github.com/dlmastery/steeringresearch/blob/master/ideas/_campaigns/E15-learned-gate.json))
- Experiment log (all rows, append-only): `autoresearch_results/experiment_log.jsonl` ([blob](https://github.com/dlmastery/steeringresearch/blob/master/autoresearch_results/experiment_log.jsonl)) — this hypothesis = exp# 110
- Reasoning annotations: `autoresearch_results/reasoning_annotations.json` ([blob](https://github.com/dlmastery/steeringresearch/blob/master/autoresearch_results/reasoning_annotations.json))
- Design doc: `hypotheses/B_conditional/E15_learned_gate_vs_fixed_threshold.md` ([blob](https://github.com/dlmastery/steeringresearch/blob/master/hypotheses/B_conditional/E15_learned_gate_vs_fixed_threshold.md))

## 4. Reasoning trace (representative)

Representative reasoning entry — **exp#110** (`autoresearch_results/reasoning_annotations.json` key `"110"`):

- **Diagnosis:** E15 was UNTESTED for lack of a gate trainer. The new steering.gate module (a gradient-trained multi-layer logistic gate + a pure-numpy PR/ROC-AUC + the fixed CosineGate baseline) now lets us screen whether a learned multi-layer gate beats a single-layer fixed cosine threshold under distribution shift, using feature layers [4, 8, 11, 14]. In-dist = direct harmful requests (jailbreak_mini); OOD = indirect/roleplay/fiction-framed requests (ood_harmful_mini), a genuine surface-form shift.
- **Hypothesis:** Mechanism: because the logistic gate combines dot products from multiple layers, it can exploit complementary harm signals (shallow syntax-level, deep semantic) that a single-layer cosine dot product misses, making the decision boundary more invariant to OOD surface form. We predict the learned gate's OOD PR-AUC exceeds the best fixed cosine threshold's by >= 0.06 (the falsifier).
- **Prediction:** OOD AUC gap (logistic - best cosine) in [-0.05, +0.30]; SUPPORTED iff >= 0.06. In-dist AUC is resubstitution at this tiny scale. n=1 SCREENING, fingerprint a9001e87087e.
- **Verdict:** DISCARD — composite -0.1679, behavior +0.5498, CR 0.0000 (global best remains +0.8336 tag=E3-cliff-a1.0)

## 5. Result & interpretation

Across these 1 screening cells the composite ranged -0.168..-0.168; perplexity 0.0..0.0; off-shell Δ‖h‖ 0.0000..0.0000. The IDEA_TABLE verdict for E15 is **FALSIFIED_OOD(scr): learned gate overfits tiny data, OOD AUC gap -0.17 (logistic 0.55 vs cosine 0.72), both perfect in-dist (exp#110)** — see the design doc's status journal and committee Q&A for the full interpretation and the pre-registered falsifier. These rows are n=1 SCREENING geometry probes (behavior is a synthetic-lexicon proxy; PPL and off-shell Δ‖h‖ are the real signals); the rung-3 generation-judge confirmation is the pending step.
