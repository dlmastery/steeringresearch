# E20 — Provenance & Tracing

> **Title:** SAE-TS vs DiffMean 3-stack coherence  
> **Verdict (IDEA_TABLE.md):** FALSIFIED(scr): SAE-TS LESS orthogonal, Gram 3.00 vs DiffMean 2.13 (vectors collapsed); tiny-real-SAE coverage confound (exp#112/113)  
> **Models touched:** gemma-3-270m-it  
> **Experiments:** 2 rows — exp# 112, 113

_Auto-generated by `scripts/build_provenance.py` from `experiment_log.jsonl` + `reasoning_annotations.json` + `ideas/_campaigns/`. Do not hand-edit; re-run the generator._

> **This hypothesis trains a small auxiliary component** (a logistic gate / an MLP hypernetwork / a sparse autoencoder + an optimized steering vector) — the base model stays frozen. These are the project's only gradient-trained experiments; the rows below carry method-specific metrics (gate AUC / held-out cosine / Gram mass) in their `method_extra`, not the standard 5-axis composite. See [`../TRAINING-PROCESS.md`](../TRAINING-PROCESS.md) §4.

## 1. The exact experiments

| exp# | tag | model | layer | alpha | op | source | behavior | PPL | composite | off-shell |
|------|-----|-------|-------|-------|----|--------|----------|-----|-----------|-----------|
| 112 | `E20-diffmean-3stack` | gemma-3-270m-it | 6 | — | add | diffmean | 0.999 | 90.3 | -0.001 | 0.0000 |
| 113 | `E20-saets-3stack` | gemma-3-270m-it | 6 | — | add | sae_ts | 1.000 | 89.4 | 0.000 | 0.0000 |

## 2. How to reproduce

**Tag prefix `E20-diffmean-3stack*`** — script `scripts/run_e20.py  (steering.sae)`, artifact `ideas/_campaigns/E20-saets.json`:

```bash
PYTHONPATH=src python scripts/run_e20.py --model models/google/gemma-3-270m-it --quant none --layer 6 # trains a sparse autoencoder + optimizes SAE-TS vectors; Gram mass + 3-stack coherence
```

**Tag prefix `E20-saets-3stack*`** — script `scripts/run_e20.py  (steering.sae)`, artifact `ideas/_campaigns/E20-saets.json`:

```bash
PYTHONPATH=src python scripts/run_e20.py --model models/google/gemma-3-270m-it --quant none --layer 6 # trains a sparse autoencoder + optimizes SAE-TS vectors; Gram mass + 3-stack coherence
```

Each campaign cell pre-authors a `_manual` reasoning entry tied to this hypothesis, then calls `steering.runner.run_single_experiment` (model loaded once via `load_model_cached`); every cell appends one row to `experiment_log.jsonl` with the shared `composite_fingerprint`.

## 3. Artifacts

- Campaign result JSON: `ideas/_campaigns/E20-saets.json` ([blob](https://github.com/dlmastery/steeringresearch/blob/master/ideas/_campaigns/E20-saets.json))
- Experiment log (all rows, append-only): `autoresearch_results/experiment_log.jsonl` ([blob](https://github.com/dlmastery/steeringresearch/blob/master/autoresearch_results/experiment_log.jsonl)) — this hypothesis = exp# 112, 113
- Reasoning annotations: `autoresearch_results/reasoning_annotations.json` ([blob](https://github.com/dlmastery/steeringresearch/blob/master/autoresearch_results/reasoning_annotations.json))
- Design doc: `hypotheses/C_stacking/E20_sae_ts_vs_diffmean_3stack.md` ([blob](https://github.com/dlmastery/steeringresearch/blob/master/hypotheses/C_stacking/E20_sae_ts_vs_diffmean_3stack.md))

## 4. Reasoning trace (representative)

Representative reasoning entry — **exp#113** (`autoresearch_results/reasoning_annotations.json` key `"113"`):

- **Diagnosis:** E20 was UNTESTED for lack of SAE infrastructure. With the new steering.sae module (a self-contained sparse autoencoder + the SAE-TS gradient-ascent vector optimizer) we can now screen the core claim: do SAE-TS vectors, optimized to activate target features while suppressing side-effect features, form a more orthogonal 3-stack than raw DiffMean vectors, at injection layer 6? This is the first run that optimizes the steering vector itself rather than extracting it closed-form.
- **Hypothesis:** Mechanism: because SAE-TS minimizes activation of non-target features, two SAE-TS vectors for different behaviors share fewer feature directions than two DiffMean vectors, so |cos| and the Gram off-diagonal mass drop; per N5 the lower joint norm of a more-orthogonal stack yields lower perplexity (better coherence). We predict gram_mass(SAE-TS) < gram_mass(DiffMean) and a non-negative coherence gap.
- **Prediction:** Gram mass reduction > 0 (SAE-TS more orthogonal); coherence gap in [-0.05, +0.20]; SUPPORTED only if gram reduction AND coherence gap >= 0.10. n=1 SCREENING, fingerprint a9001e87087e.
- **Verdict:** DISCARD — composite +0.0000, behavior +1.0000, CR 0.0000 (global best remains +0.8336 tag=E3-cliff-a1.0)

## 5. Result & interpretation

Across these 2 screening cells the composite ranged -0.001..+0.000; perplexity 89.4..90.3; off-shell Δ‖h‖ 0.0000..0.0000. The IDEA_TABLE verdict for E20 is **FALSIFIED(scr): SAE-TS LESS orthogonal, Gram 3.00 vs DiffMean 2.13 (vectors collapsed); tiny-real-SAE coverage confound (exp#112/113)** — see the design doc's status journal and committee Q&A for the full interpretation and the pre-registered falsifier. These rows are n=1 SCREENING geometry probes (behavior is a synthetic-lexicon proxy; PPL and off-shell Δ‖h‖ are the real signals); the rung-3 generation-judge confirmation is the pending step.
