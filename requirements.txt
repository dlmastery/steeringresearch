# Steering research harness — dependencies.
# Torch is installed separately with the correct CUDA wheel (cu124 on the 4090):
#   pip install torch==2.6.0 --index-url https://download.pytorch.org/whl/cu124
torch>=2.6
transformers>=4.55
bitsandbytes>=0.43
accelerate>=0.30
numpy>=1.26
scipy>=1.11
pandas>=2.0
scikit-learn>=1.3
datasets>=2.18
pytest>=8.0
matplotlib>=3.8
