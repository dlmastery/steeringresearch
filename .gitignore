# Python
__pycache__/
*.py[cod]
.venv/
venv/
*.egg-info/
.pytest_cache/
.mypy_cache/
.ruff_cache/

# Model weights / caches (use HF hub, never commit)
*.pt
*.safetensors
*.bin
*.gguf
hf_cache/
.cache/
activation_cache/
*.npy
*.npz

# Large run artifacts (logs/JSON are kept; raw tensors are not)
autoresearch_results/**/activations/
autoresearch_results/**/*.pt

# Secrets
.env
*.token
hf_token.txt
.gemini_key
*api_key*
*apikey*
gemini_key.txt

# OS
.DS_Store
Thumbs.db

# Keep these explicitly
!autoresearch_results/experiment_log.jsonl
!autoresearch_results/best_config.json

.hf_token

# downloaded model weights/tokenizers (re-fetch via scripts/hf_fetch.py)
models/
*.parquet

# Gemini judge response cache (regenerable; results are in the campaign artifacts)
autoresearch_results/judge_cache/
.playwright-mcp/
.dash_v2/
.dircache_*.npy
steering_tutorials/*/artifacts/_run_checkpoint.json

# Embedding caches computed under transformers 4.55 with EmbeddingGemma silently
# running CAUSAL (audits/AUDIT_2026-08-17_embeddinggemma_causal.md). Invalid, and
# regenerable -- kept out of the tree rather than shipped as if they were evidence.
_causal_invalid_caches/
steering_tutorials/traj_probes/artifacts/corpus_*.json.gz
steering_tutorials/traj_probes/artifacts/_stale_prefix_bundles/
steering_tutorials/traj_probes/artifacts/acts_*.npz*
